{
  "name": "casknews",
  "version": "0.1.0",
  "description": "Discover Homebrew casks from your terminal — powered by cask.news",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "casknews": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm --minify && printf '#!/usr/bin/env node\\n' | cat - dist/index.js > dist/tmp && mv dist/tmp dist/index.js && chmod +x dist/index.js",
    "dev": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.14",
    "react-devtools-core": "^7.0.1"
  },
  "dependencies": {
    "boxen": "^8.0.1",
    "chalk": "^5.6.2",
    "cli-table3": "^0.6.5",
    "commander": "^14.0.3",
    "ink": "^6.8.0",
    "open": "^11.0.0",
    "ora": "^9.3.0",
    "react": "^19.2.4"
  }
}
